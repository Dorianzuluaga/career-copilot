# Database

This directory is reserved for database-level documentation and future approved
PostgreSQL resources.

The bootstrap does not include schemas, migrations, seed data, or business
models. Prisma configuration will be owned by `apps/api/prisma` in a later
bootstrap step, as defined by the engineering decisions.
